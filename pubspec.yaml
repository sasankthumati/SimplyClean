name: simply_clean
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0    # Updated to ensure compatibility
  firebase_auth: ^5.3.1      # This version should be compatible
  cloud_firestore: ^5.4.4    # Ensure this is compatible with firebase_core
  geolocator: ^9.0.2
  geocoding: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
